# D2RE Command Center

The D2RE Command Center is the default GUI layer for the toolkit. It is a local, standalone browser interface generated by `d2re` or `d2re gui`.

The interface is intentionally read-only. It helps users discover workflows, build valid command lines, copy known-good commands, and understand which subsystem to use without adding a long-running service, database, or unsafe file mutation path.

## Startup behavior

Running the package entry point with no subcommand now opens the GUI automatically:

```bash
d2re
```

Terminal workflows still work through explicit subcommands:

```bash
d2re parse MyChar.d2s --json
d2re roll --seed 0xDEADBEEF --ilvl 85 --mf 300
d2re tc --tc "Act 5 Super C" --resolve --top 25
```

## Launch commands

Generate and open the command center:

```bash
d2re
```

Or explicitly:

```bash
d2re gui
```

Show CLI help without launching the GUI:

```bash
d2re --no-gui
```

Start the server without opening a browser:

```bash
d2re gui --no-open --print-path
```

Generate static HTML instead of starting the interactive server:

```bash
d2re gui --out ./d2re-command-center.html --no-open
```

Use the dedicated entry point:

```bash
d2re-gui
```

## Integrated command builders

Every public command surface has a GUI builder:

| Builder | Purpose |
|---|---|
| Save Parser | Build `.d2s` parsing, item listing, checksum, and JSON commands. |
| Item Roller | Build deterministic seed, Magic Find, Treasure Class, and affix commands. |
| Data Extractor | Build MPQ/CASC extraction, table export, string extraction, and TC-tree commands. |
| Packet Sniffer | Build demo, verbose, live capture, PCAP decode, filter, output, list, and struct-generation commands. |
| Map Seed Tool | Build save-seed, explicit-seed, level, ASCII, rooms, BSP, and brute-force commands. |
| Treasure Class Explorer | Build TC inspection, resolution, top-N, and JSON commands. |
| Drop Calculator | Build Monte Carlo drop-estimation commands. |
| GUI Launcher | Build command-center output/opening commands. |
| Repository Doctor | Shows the planned diagnostic surface while `d2re doctor` remains disabled. |

The browser never executes these commands. It only previews and copies them so the user can run them deliberately in a terminal.

## Extra functionality

The command center adds more than the original module cards:

- command builders for all public commands
- command preview that updates as form fields change
- copy buttons for generated commands
- workflow recipes with multi-command scripts
- search and status filtering
- module cards linked to their builders
- startup guidance explaining that plain `d2re` opens the GUI

## UX and design decisions

This GUI applies the uploaded UI/UX skill pack as a practical implementation checklist.

### Accessibility

- Visible skip link for keyboard users.
- Sequential headings.
- Full keyboard focus visibility with a high-contrast focus ring.
- Controls meet a 44px minimum interaction target.
- Output regions use polite live-region behavior.
- Motion respects `prefers-reduced-motion`.
- The GUI is useful without pointer-only interactions.

### Layout

- Responsive two-column desktop layout.
- Single-column layout on smaller screens.
- Sticky module navigation on large screens.
- Search and status filters for progressive disclosure.
- Builder panels use grouped, labeled form fields.

### Visual system

- Tokenized CSS variables for primitives, semantic surfaces, focus, spacing, radius, and typography.
- Dark gothic treatment with obsidian surfaces, blood-red accents, parchment text, and metal-like panel borders.
- No external images, fonts, frameworks, or third-party scripts.

### Safety

- The GUI does not execute commands.
- The GUI does not edit save files.
- The GUI does not start packet capture.
- The GUI does not read local paths from the browser.
- The GUI only writes a standalone HTML file and optionally opens it in the default browser.

## Implementation notes

`d2re/gui.py` is a compatibility wrapper. The integrated implementation lives in `d2re/gui_integrated.py`.

Main structures and functions:

| Name | Purpose |
|---|---|
| `WorkbenchCard` | Discovery panel data. |
| `CommandSpec` | Builder definition for a command surface. |
| `CommandField` | One generated form control for a command. |
| `WorkflowRecipe` | Multi-command workflow snippet. |
| `build_model()` | Builds all card, builder, and recipe data. |
| `render_workbench()` | Renders standalone HTML. |
| `write_workbench()` | Writes the HTML file. |
| `main()` | GUI CLI entry point. |

Smoke tests live in `tests/test_gui_workbench.py`.

## Next GUI steps

- Add local JSON import for parser output.
- Add schema-aware result viewers once stable schemas exist.
- Add a local-only preview server only if static HTML becomes too limiting.
- Add richer guided workflows for common research tasks.
- Add a future execution bridge only after a safety and permissions model exists.
